

CONTAINER_NAME="claude-a"
BASE_DIR="/Users/zhangxinyu/claude code/201"

mkdir -p "$BASE_DIR" && \
RUN_DIR="$(mktemp -d "$BASE_DIR/run-XXXXXXXX")" && \
mkdir "$RUN_DIR/workspace" && \
printf '容器：%s\n本题本地目录：%s\n' "$CONTAINER_NAME" "$RUN_DIR" && \
docker run -it --init --restart=no \
  --cap-drop ALL \
  --security-opt no-new-privileges \
  --name "$CONTAINER_NAME" \
  --mount "type=bind,src=$RUN_DIR/workspace,dst=/workspace" \
  -e "apikey=xxxxx" \
  adminfather/benzhi-claude-code:20260909-isolated-git